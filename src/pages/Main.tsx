import CounterContainer from "../container/CounterContainer";

const Main = () => {
    return (
        <div>
            <div>Main 페이지입니다.</div>
            <CounterContainer/>
        </div>
    );
};

export default Main;